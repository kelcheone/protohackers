version: '3.7'

services:
  smoke-test:
    # image: golang:1.19.3-buster
    # container_name: smoke-test
    # working_dir: /app
    # ports:
    #   - 8080:8080
    # volumes:
    #   - .:/app
    # command: go run main.go
    build: .
    container_name: smoke-test
    ports:
      - 8080:8080
    volumes:
      - .:/app
    command: go run main.go
